DO $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN
        SELECT tablename
        FROM pg_tables
        WHERE schemaname = 'opd'
    LOOP
        EXECUTE format('DROP TABLE opd.%I CASCADE;', r.tablename);
    END LOOP;
END $$;